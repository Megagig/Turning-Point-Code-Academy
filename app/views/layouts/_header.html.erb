<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/">Turning Point Code Academy</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <% if current_user %>
      <ul class="navbar-nav mr-auto">
        <li class="nav-item <%= 'active font-weight-bold' if current_page?(root_path) %>">
          <%= link_to root_path, class: 'nav-link' do %>
            <i class="fa fa-home"></i>
            Home
          <% end %>
        </li>
        <li class="nav-item <%= 'active font-weight-bold' if current_page?(courses_path) %>">
          <%= link_to courses_path, class: 'nav-link' do %>
            <i class="fa fa-book"></i>
            Courses
            <li class="nav-item <%= 'active font-weight-bold' if current_page?(new_course_path) %>">
       <%= link_to new_course_path, class: 'nav-link' do %>
        <span class="fa fa-plus"></span>
           New Course
          <% end %>
          </li>
<li class="nav-item <%= 'active font-weight-bold' if current_page?(users_path) %>">
      <%= link_to users_path, class: 'nav-link' do %>
    <i class="fa fa-users"></i> Users
       <% end %>
          <% end %>
        </li>
      </ul>
      <!-- SEARCH IN NAVBAR -->
   <%= search_form_for @ransack_courses, as: :courses_search, url: courses_path do |f| %>
  <div class="input-group">
    <%= f.search_field :title_cont, class: 'form-control-sm', style: 'float: left; width: 150px;', placeholder: 'Find a course', autocomplete: :off %>
    <span class="input-group-append">
      <button class="btn btn-primary btn-sm" type="submit">
        <span class="fa fa-search" aria-hidden="true"></span>
      </button>
    </span>
     </div>
    <% end %>

      <ul class="navbar-nav ml-auto">
        <li class="nav-item <%= 'active font-weight-bold' if current_page?(root_path) %>">
          <%= link_to root_path, class: 'nav-link' do %>
            <i class="fa fa-home"></i>
            Another link
          <% end %>
        </li>
        <li class="nav-item">
          <%= select_tag :user_options, options_for_select([
            ['Account', edit_user_registration_path],
            ['Sign out', destroy_user_session_path]
          ], selected: params[:user_options]), onchange: 'window.location = this.value;', class: 'form-control' %>
        </li>
      </ul>
    <% else %>
      <ul class="navbar-nav ml-auto">
        <li class="nav-item"><%= link_to 'Sign Up', new_user_registration_path, class: 'nav-link' %></li>
        <li class="nav-item"><%= link_to 'Log In', new_user_session_path, class: 'nav-link' %></li>
      </ul>
    <% end %>
  </div>
</nav>